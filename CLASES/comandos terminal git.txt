pwd: me dice en que directorio/carpeta estoy.

ls: ver el contenido de la carpeta/directorio.

touch (nombre.tipo de archivo): crear un archivo en el directorio.

clear: limpia la consola de todos los comandos anteriores.

rm (nombre): borrar una carpeta/archivo.

cd (lugar donde quiero moverme): se utiliza para cambiar el directorio.

cd ..: vuelve un directorio hacia atras.

mkdir (nombre): crea un directorio.

para no hacer dos veces un cd se hace por ejm. cd facultad/python

code .: abre el visual studio code en ese directorio.

cat (nombre del archivo): muestra en la terminal el contenido de un archivo.

python --version: nos deja ver la version de python que tenemos.

python (nombre del programa): ejecuta el programa en la consola.

ls -a: nos deja ver carpetas ocultas en un directorio.

GIT:

git init: crea un repositorio local de git y nos permite ejecutar los demas comandos de git.

git status: se ve informacion del repositorio.

git add (nombre del programa): trakea/añade un archivo al repositorio para que se le pueda hacer commit.

git commit-m "": crea un commit con un mensaje.

git log: nos permite ver el historial de las modificaciones guardadas.

git add .: agrega para hcer commit a todos los archivos trakeados.

git diff (nombre del archivo): permite ver las modificaciones que reibio un archivo desde la ultima vez que se le hizo commit.

en github hay que ir al + al lado del perfil y crear un repositorio con un nombre y una descripcion.

github y git aceptan archivos de hasta 25 mb

VER GUIA EN EL PORTAL PARA NO PERDERSE!!!!!





